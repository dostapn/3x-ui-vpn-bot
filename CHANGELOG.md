# Changelog

Все значимые изменения в проекте документируются в этом файле.

## [1.3.0] - 2026-01-28

### ✨ Улучшение UX и исправление Reality

#### Добавлено
- **Система уведомлений об обновлениях:** Бот автоматически уведомляет администратора о выходе новых версий при запуске.
- **Инфо для админа:** При получении запроса на ключ теперь отображается статистика пользователя (сколько ключей уже выдано и их список с названиями inbound и статусом).
- **Умный парсинг CHANGELOG:** Бот умеет извлекать историю изменений для уведомлений.
- **Унификация обратной связи:** Все напоминания о связи с админом вынесены в единый шаблон.
- **Оптимизация сессий:** Устранено дублирование инициализации Bot в хендлерах (используется контекстный бот).
- **Подсветка синтаксиса:** Уведомления об обновлениях теперь приходят с Markdown-подсветкой (`language-markdown`).
- **Статистика в списке запросов:** Команда `/requests` теперь также показывает детальную информацию о текущих ключах пользователя.

#### Исправлено
- **VLESS Reality ссылки:** Исправлена генерация ссылок, теперь все параметры (`pbk`, `fp`, `sni`, `sid`, `spx`) корректно включаются в URL.
- **Форматирование сообщений:** VLESS ссылки теперь отображаются в удобном блоке для копирования (`<pre><code>`) с типом `language-text`.
- **Моноширинный текст:** Все Email, ID и технические идентификаторы теперь выделены шрифтом `<code>`.
- **Инструкции по подключению:** Обновлен список рекомендуемых клиентов (Hiddify, v2rayTun, NekoBox, FoXray, v2rayN, v2rayNG) с иконками платформ и прямыми ссылками на сайты и магазины приложений.
- **Дубликаты уведомлений:** Реализовано сохранение версии в БД после каждого отправленного сообщения.

#### Изменено
- **База данных:** Добавлена таблица `bot_settings` для хранения системных параметров.
- **Конфигурация:** Добавлена константа версии бота.
- **Клиентские сообщения:** Улучшены тексты в командах `/start`, `/help` и `/id`.
- **README.md:** Добавлены четкие инструкции по запуску в Docker и Standalone режиме.

---

## [1.2.1] - 2026-01-22

### 🐛 Исправлено

- **py3xui SSL поддержка:** Реализована поддержка самоподписанных сертификатов через monkey-patching requests
- **Валидация URL:** `XUI_HOST` теперь требует явного указания протокола (`http://` или `https://`)
- **SSL verification:** Добавлен параметр `XUI_USE_SSL_CERT` для отключения проверки сертификатов

### 🔧 Технические детали

- Используется monkey-patching `requests.Session.request` для передачи `verify=False`
- Отключены warnings от urllib3 для самоподписанных сертификатов
- Патч применяется только при `XUI_USE_SSL_CERT=false`
- py3xui.Api не поддерживает параметр `use_ssl_certificate`, поэтому используется обходное решение

### 📝 Документация

- Улучшены комментарии в `.env.example` для HTTPS конфигурации
- Обновлена секция "Конфигурация" в README.md с примерами для HTTPS
- Добавлены примеры для разных сценариев (HTTP, HTTPS, кастомные пути)

### ⚠️ Breaking Changes

- **`XUI_HOST` теперь ДОЛЖЕН включать протокол**
  - ❌ Старый формат: `localhost:8443` или `127.0.0.1:2053`
  - ✅ Новый формат: `http://127.0.0.1:2053` или `https://localhost:8443/path/`
- Для HTTPS с самоподписанными сертификатами установите `XUI_USE_SSL_CERT=false`

### Примеры конфигурации

```bash
# HTTP (стандартный 3x-ui)
XUI_HOST=http://127.0.0.1:2053
XUI_USE_SSL_CERT=true

# HTTPS с самоподписанным сертификатом
XUI_HOST=https://localhost:8443/dostapn-3x-ui/
XUI_USE_SSL_CERT=false
```

---

## [1.2.0] - 2026-01-22

### 🚀 Production-Ready Release

#### Подтверждено
- **Standalone деплой на VDS** - проект полностью готов к работе рядом с 3x-ui на одном сервере
- **systemd service** - рекомендуемый способ для продакшн (файл `vpn-bot.service` включен)
- **Легковесность** - потребление ~80-100 MB RAM, <1% CPU в idle
- **Прямая интеграция** - доступ к локальной БД 3x-ui (`/etc/x-ui/x-ui.db`)
- **Простое управление** - через `systemctl start/stop/restart vpn-bot`
- **Централизованные логи** - через `journalctl -u vpn-bot -f`

#### Рекомендации по деплою
- **Docker Compose** - только для локальной разработки и тестирования
- **systemd service** - для продакшн на VDS (установка ~5 минут)
- **Преимущества standalone:**
  - Нет overhead от Docker
  - Прямой доступ к БД 3x-ui без volume mapping
  - Проще интеграция с системными сервисами
  - Меньше потребление ресурсов

#### Изменено
- **Документация:** Удалено дублирование CHANGELOG из README.md
- **README.md:** Раздел Changelog заменен на ссылку (принцип DRY)

---

## [1.1.0] - 2026-01-22

### ✨ Двусторонняя связь через бота

#### Добавлено
- **Reply-ответы от админа:** Админ может отвечать на сообщения пользователей через обычный Reply в Telegram
- **Цитаты в reply:** Когда клиент отвечает на сообщение бота, админ видит цитату оригинального сообщения
- **Автоматическое определение получателя:** Бот извлекает ID пользователя из пересланного сообщения (5 вариантов regex)
- **Поддержка пользователей без username:** Reply работает для всех пользователей
- **Защита от зацикливания:** Сообщения админа не пересылаются обратно

#### Изменено
- Порядок регистрации роутеров: `admin_router` первым для обработки reply
- Фильтр admin reply: добавлена проверка `F.from_user.id == config.admin_id`
- Удалена приписка "Ответ придёт в этот же чат" из подтверждения отправки
- Все комментарии в коде переведены на русский язык (70+ комментариев)

#### Исправлено
- Reply от админа теперь корректно доходят до пользователей
- Reply от клиентов не перехватываются admin handler'ом
- Длинные строки в коде разбиты для соответствия flake8 (≤100 символов)

#### Документация
- Обновлён README.md с описанием двусторонней связи
- Обновлён BOT_SCENARIOS.md (добавлены сценарии 4 и 15)
- Добавлен пример 4: "Полный диалог пользователь ↔ админ"

---

## [1.0.1] - 2026-01-22

### ✨ Обновление: Улучшенная обратная связь

#### Добавлено
- **Reply-ответы от админа:** Админ может отвечать на сообщения пользователей через обычный Reply в Telegram (как в обычном чате)
- **Пересылка сообщений:** Любое текстовое сообщение от пользователя автоматически пересылается админу
- **Двусторонняя связь:** Полноценный диалог между пользователем и админом через бота

#### Изменено
- Улучшена обработка текстовых сообщений от пользователей
- Добавлена проверка блокировки при отправке сообщений
- Обновлена документация с новым сценарием (Сценарий 15)

---

## [1.0.0] - 2026-01-22

### 🎉 Первый релиз

#### Добавлено

**Клиентская функциональность:**
- `/start` команда с главным меню и подсказкой об обратной связи
- `/help` - справка с командами (для админа показываются дополнительные команды)
- `/status` - проверка статуса бота
- `/id` - показ Telegram ID пользователя
- Просмотр всех ключей с VLESS конфигами (Reality параметры)
- Запрос новых ключей с защитой от дублирования
- Просмотр статистики использования трафика
- Автоматические уведомления при одобрении запросов
- Отправка текстовых сообщений админу (пересылка)

**Административная функциональность:**
- Выдача новых ключей с выбором inbound или клонированием
- Присвоение существующих ключей пользователям
- `/requests` - просмотр всех незавершённых запросов с кнопками действий
- `/keys` - просмотр всех ключей с привязанными пользователями
- `/bans` - список заблокированных пользователей
- `/unban_ID` - разблокировка пользователя
- Управление пользователями ключа:
  - Отвязка ключа от пользователя
  - Блокировка пользователя на 24 часа
- Два типа отклонения запросов:
  - С блокировкой на 24ч
  - Без блокировки
- Отправка сообщений пользователям:
  - Через кнопку "💬 Написать сообщение"
  - Через Reply на сообщения пользователей (v1.0.1)

**База данных:**
- Таблица `telegram_users` для хранения пользователей
- Таблица `user_keys` для связи пользователь-ключ (один ключ → много пользователей)
- Таблица `pending_requests` для очереди запросов
- Интеграция с существующей БД 3x-ui
- Функции для подсчёта пользователей по ключу
- Функции для получения всех ключей с пользователями

**API интеграция:**
- Полная интеграция с 3x-ui REST API через py3xui 0.5.5
- Генерация VLESS URL с полными Reality параметрами:
  - type, encryption, security
  - pbk (public key), fp (fingerprint)
  - sni (server name), sid (short ID)
  - spx (spider X)
- Управление inbound'ами (получение, создание, клонирование)
- Управление клиентами (создание, получение, удаление)
- Получение статистики трафика

**Развертывание:**
- Docker Compose для локальной разработки
- systemd service для продакшн деплоя
- Автоматическая инициализация БД
- Централизованное логирование
- .gitignore и .dockerignore для правильной работы с Git и Docker

**Документация:**
- Единый README.md со всей необходимой информацией:
  - Быстрый старт
  - Установка (Docker и продакшн)
  - Конфигурация
  - Использование
  - Архитектура
  - Troubleshooting
- BOT_SCENARIOS.md - детальные сценарии взаимодействия (14 сценариев)
- AI_PROMPT_TEMPLATE.md - шаблоны промптов для создания публикаций
- CHANGELOG.md - история изменений

#### Технические детали

**Стек технологий:**
- Python 3.10+
- aiogram 3.13.1 (Telegram Bot Framework)
- py3xui 0.5.5 (3x-ui API Client)
- SQLite (встроенная БД 3x-ui)
- Docker & Docker Compose

**Архитектура:**
- Модульная структура с разделением handlers
- FSM (Finite State Machine) для сложных диалогов
- Централизованные шаблоны сообщений (`bot/utils/messages.py`)
- Централизованные клавиатуры (`bot/utils/keyboards.py`)
- Обёртка для 3x-ui API (`bot/utils/xui_api.py`)
- Контекстные менеджеры для работы с БД
- Обработка ошибок и детальное логирование

**Безопасность:**
- Проверка прав администратора на всех admin-хендлерах
- Автоматическая блокировка при отклонении запросов
- Валидация входных данных
- Защита .env файла (chmod 600)
- Использование существующей БД 3x-ui (нет дублирования данных)

**Производительность:**
- Потребление ~80-100 MB RAM
- <1% CPU в idle режиме
- Асинхронная обработка запросов

**UX улучшения:**
- Защита от дублирования запросов (find or create)
- Предупреждение о приватности VPN в каждом сообщении с ключом
- Инструкция по подключению только в финальном сообщении
- Универсальная инструкция для v2rayNG / Hiddify / другие клиенты
- Отображение количества пользователей на каждом ключе
- Минимальное количество кнопок (только необходимые)

### Известные ограничения

- QR-коды для конфигов пока не реализованы (можно добавить по запросу)
- Поддержка только одного администратора
- Лимиты трафика используют настройки 3x-ui (настраиваются через панель)
- Polling режим (webhook можно добавить при необходимости)

### Примечания

Проект полностью функционален и готов к использованию. Дополнительные функции могут быть добавлены по мере необходимости.

---

## Формат версий

Проект использует [Semantic Versioning](https://semver.org/):
- MAJOR версия - несовместимые изменения API
- MINOR версия - новая функциональность (обратно совместимая)
- PATCH версия - исправления ошибок

## Типы изменений

- **Добавлено** - новая функциональность
- **Изменено** - изменения в существующей функциональности
- **Устарело** - функциональность, которая будет удалена
- **Удалено** - удаленная функциональность
- **Исправлено** - исправления ошибок
- **Безопасность** - исправления уязвимостей
